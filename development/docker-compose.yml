services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: plates_dev
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: plates_dev
    ports:
      - "5432:5432"
    volumes:
      - plates_dev_data:/var/lib/postgresql/data

volumes:
  plates_dev_data: